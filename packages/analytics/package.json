{
  "name": "@ecommerce/analytics",
  "version": "0.0.1",
  "description": "package to track page and user events",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/src/index.d.ts",
  "scripts": {
    "build": "tsup && tsc --emitDeclarationOnly",
    "watch": "pnpm build",
    "lint": "eslint \"**/*.ts*\"",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@ecommerce/eslint-config": "workspace:*",
    "@ecommerce/typescript-config": "workspace:*",
    "@types/jsdom": "^21.1.6",
    "@types/node": "^20.14.11",
    "eslint": "^8.57.1",
    "jsdom": "^24.0.0",
    "tsup": "^8.0.2",
    "typescript": "^5.6.2",
    "vitest": "^2.1.2"
  }
}
